version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: emuverse-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: emuverse
      POSTGRES_USER: emuverse
      POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - emuverse-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U emuverse"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis
  redis:
    image: redis:7-alpine
    container_name: emuverse-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - emuverse-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: emuverse-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://emuverse:${DB_PASSWORD:-changeme}@postgres:5432/emuverse
      REDIS_URL: redis://redis:6379
      JWT_SECRET: ${JWT_SECRET}
      PORT: 4000
    volumes:
      - ./data/roms:/opt/emuverse/data/roms
      - ./data/bios:/opt/emuverse/data/bios
      - ./data/saves:/opt/emuverse/data/saves
      - ./data/covers:/opt/emuverse/data/covers
      - ./data/screenshots:/opt/emuverse/data/screenshots
    networks:
      - emuverse-network
    ports:
      - "4000:4000"

  # Frontend (built and served by nginx in production)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: emuverse-frontend
    restart: unless-stopped
    networks:
      - emuverse-network

  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: emuverse-nginx
    restart: unless-stopped
    depends_on:
      - backend
      - frontend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./docker/nginx/ssl:/etc/nginx/ssl:ro
      - frontend_dist:/usr/share/nginx/html:ro
      - ./data/covers:/usr/share/nginx/static/covers:ro
      - ./data/screenshots:/usr/share/nginx/static/screenshots:ro
    networks:
      - emuverse-network

volumes:
  postgres_data:
  redis_data:
  frontend_dist:

networks:
  emuverse-network:
    driver: bridge

version: '3.8'

services:
  # ===========================================
  # PostgreSQL Database
  # ===========================================
  postgres:
    image: postgres:15-alpine
    container_name: emuverse-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-emuverse}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-emuverse_secret}
      POSTGRES_DB: ${DB_NAME:-emuverse}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-emuverse}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - emuverse-network

  # ===========================================
  # Redis Cache
  # ===========================================
  redis:
    image: redis:7-alpine
    container_name: emuverse-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - emuverse-network

  # ===========================================
  # Backend API Server
  # ===========================================
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: emuverse-backend
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: postgresql://${DB_USER:-emuverse}:${DB_PASSWORD:-emuverse_secret}@postgres:5432/${DB_NAME:-emuverse}
      REDIS_URL: redis://redis:6379
      JWT_SECRET: ${JWT_SECRET:-change-this-to-a-secure-secret}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-7d}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost}
      UPLOAD_DIR: /app/data
      RA_API_KEY: ${RA_API_KEY:-}
    volumes:
      - emuverse_data:/app/data
      - emuverse_logs:/app/logs
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - emuverse-network

  # ===========================================
  # Frontend Web Server
  # ===========================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-/api}
    container_name: emuverse-frontend
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - backend
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ssl_certs:/etc/nginx/ssl:ro
    networks:
      - emuverse-network

  # ===========================================
  # Streaming Server (WebRTC)
  # ===========================================
  streaming:
    build:
      context: ./backend
      dockerfile: Dockerfile.streaming
    container_name: emuverse-streaming
    restart: unless-stopped
    environment:
      PORT: 8080
      REDIS_URL: redis://redis:6379
      STUN_SERVER: ${STUN_SERVER:-stun:stun.l.google.com:19302}
    ports:
      - "8080:8080"
      - "10000-10100:10000-10100/udp"
    depends_on:
      - redis
    networks:
      - emuverse-network

# ===========================================
# Networks
# ===========================================
networks:
  emuverse-network:
    driver: bridge

# ===========================================
# Volumes
# ===========================================
volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  emuverse_data:
    driver: local
  emuverse_logs:
    driver: local
  ssl_certs:
    driver: local
